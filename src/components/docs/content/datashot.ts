import type { DocSection } from '../docsData'

export const datashotSection: DocSection = {
  id: 'datashot',
  title: 'DataShot',
  pages: [
    {
      id: 'datashot-overview',
      title: '데이터셋 개요',
      slug: 'datashot-overview',
      updatedAt: '2026-05-28',
      content: `# 데이터셋 개요

## DataShot이란?

DataShot은 업종별 광고 성과 벤치마크 데이터를 추출하는 솔루션입니다.

매체·기간·업종·광고상품·지표 등 조회 조건을 조합하면, 내부 적재 데이터에서 조건에 부합하는 캠페인 성과 데이터를 추출합니다. 추출 결과물을 "데이터셋"이라 합니다.

## 주요 목적

광고상품별 업종 벤치마크를 기반으로 예상 성과 효율을 사전에 산정하고, 미디어 플래닝의 의사결정 근거로 활용합니다.

## 주요 기능

- **데이터셋 생성** — 조회 조건을 설정하여 성과 데이터를 추출합니다.
- **데이터셋 관리** — 생성된 데이터셋을 이동·복제·삭제합니다.
- **데이터셋 결과** — 추출된 데이터셋을 테이블로 조회하고 CSV로 내보냅니다.

## 지원 매체

- Google Ads
- Meta
- kakao모먼트
- 네이버 성과형 DA
- 네이버 보장형 DA
- TikTok

## 워크플로우

1. DataShot 목록에서 "New Dataset"을 클릭합니다.
2. 3단계 스텝 폼으로 조회 조건을 설정합니다.
3. 생성을 요청하면 조건에 맞는 성과 데이터가 추출됩니다.
4. 추출 완료 후 목록에서 데이터셋을 클릭하여 결과를 확인합니다.

## 접근 권한

| 역할 | 데이터셋 조회 | 데이터셋 생성 | 이동/복제/삭제 |
|---|---|---|---|
| Admin | O | O | O |
| Marketer | O | O | O |
| Client | O | X | X |
| Agency | O | X | X |

## 데이터 업데이트 주기

매월 5일경 전월 데이터를 정기 업데이트합니다. 데이터 품질 향상을 위해 최근 2년 내 과거 데이터의 보정이 비정기적으로 진행될 수 있습니다.

## Quota 정책

- Slot당 최대 100개의 유효 데이터셋을 보유할 수 있습니다.
- 만료(Expired) 상태의 데이터셋은 Quota에 포함되지 않습니다.
- Quota 한도 도달 시 새 데이터셋을 생성할 수 없습니다.`
    },
    {
      id: 'datashot-create',
      title: '데이터셋 생성',
      slug: 'datashot-create',
      updatedAt: '2026-05-28',
      content: `# 데이터셋 생성

DataShot 목록 화면에서 "New Dataset" 버튼을 클릭하면 데이터셋 생성 화면으로 진입합니다. 3단계 스텝 폼을 순서대로 완료하여 데이터셋을 생성합니다.

## 화면 구성

화면은 좌측 스텝 폼 영역과 우측 Configuration Summary 영역으로 나뉩니다. Configuration Summary는 현재까지 설정한 조회 조건을 실시간으로 요약하여 보여줍니다.

---

## Step 1: 기본 정보

첫 번째 단계에서는 데이터셋의 기본 정보를 입력합니다.

### 데이터셋명 (필수)

- 데이터셋의 이름을 입력합니다.
- 최대 30자까지 입력 가능합니다.
- 공백만으로 구성할 수 없습니다.

### 설명 (선택)

- 데이터셋에 대한 부가 설명을 입력합니다.
- 최대 200자까지 입력 가능합니다.

### 조회기간 (필수)

- 데이터를 조회할 기간을 설정합니다.
- 최근 2년 이내의 데이터만 조회할 수 있습니다.
- 기간 유형을 "월별" 또는 "분기별"로 선택할 수 있습니다.
- 월별 선택 시: yyyy-mm 형식으로 시작월과 종료월을 지정합니다.
- 분기별 선택 시: yyyy-Qn 형식으로 시작분기와 종료분기를 지정합니다.
- 기간 유형을 변경하면 기존 선택값이 초기화됩니다.

### 업종 (필수)

- 분석 대상 업종을 선택합니다.
- "업종 선택" 영역을 클릭하면 업종 선택 다이얼로그가 열립니다.
- 분류 레벨(대분류/중분류)을 먼저 선택한 후, 해당 레벨의 업종을 선택합니다.
- 다중 선택이 가능합니다.
- 업종/브랜드 검색 기능을 활용하여 원하는 업종을 빠르게 찾을 수 있습니다.
- 분류 레벨을 변경하면 기존 선택한 업종이 초기화됩니다.

---

## Step 2: 상세 설정

두 번째 단계에서는 매체, 광고상품, 타겟팅 옵션, 지표를 설정합니다.

### 매체 (필수)

- 분석할 광고 매체를 선택합니다.
- 단일 선택만 가능합니다.
- 매체를 변경하면 하위 설정(광고상품, 타겟팅, 지표)이 모두 초기화됩니다.

### 광고상품 (필수)

- 매체를 선택하면 해당 매체의 광고상품 분류 필드가 표시됩니다.
- Step 1에서 설정한 기간과 업종에 해당하는 데이터가 존재하는 옵션만 표시됩니다.
- 첫 번째 필드는 최소 1개 이상 선택해야 합니다.
- 나머지 필드는 선택 사항입니다.
- 검색 기능으로 옵션을 빠르게 찾을 수 있습니다.
- 매체별 광고상품 필드 구성:
  - Google Ads: 캠페인 유형, 캠페인 하위 유형, 입찰 전략 유형, 광고그룹 유형, 광고 유형
  - Meta: 캠페인 목표, 구매 유형, 플랫폼, 성과 목표
  - kakao모먼트: 광고 목표, 광고 유형, 광고 목표 설정, 입찰 방식, 소재 유형
  - 네이버 성과형 DA: 캠페인 목적, 청구 기준, 게재 위치
  - 네이버 보장형 DA: 광고상품명, 상품 유형, 과금 방식
  - TikTok: 목표, 최적화 목표, 게재위치, 광고 형식

### 타겟팅 옵션 (선택)

- 매체에 따라 타겟팅 옵션을 추가로 설정할 수 있습니다.
- 카테고리를 먼저 선택한 후, 해당 카테고리의 옵션을 선택합니다.
- 카테고리를 변경하면 기존 선택한 옵션과 지표가 초기화됩니다.
- 매체별 타겟팅 카테고리:
  - Google Ads: 기기유형
  - Meta: 기기유형
  - kakao모먼트: 성별, 연령, 성별x연령, 지역, 디바이스, 게재지면
  - 네이버 성과형 DA: 타겟팅 옵션 없음
  - 네이버 보장형 DA: 노출영역, 시간
  - TikTok: 플랫폼, 성별, 연령, 성별X연령, 국가

### 지표 (필수)

- 추출할 지표를 선택합니다.
- 매체와 타겟팅 옵션 조합에 따라 조회 가능한 지표만 목록에 표시됩니다.
- 검색 기능으로 지표를 빠르게 찾을 수 있습니다.
- 그룹별로 분류되어 있어 원하는 지표를 쉽게 선택할 수 있습니다.

---

## Step 3: 검토 및 추출

세 번째 단계에서는 설정 내용을 최종 확인하고 데이터셋 생성을 요청합니다.

### 설정 확인

- 우측 Configuration Summary에서 전체 설정 내용을 확인합니다.

### 데이터 미리보기

- 샘플 데이터 5행을 테이블 형태로 미리 확인할 수 있습니다.
- "전체 컬럼 보기" 버튼을 클릭하면 모든 컬럼을 포함한 전체 미리보기를 확인할 수 있습니다.
- 설정한 조건에 맞는 데이터가 없으면 빈 상태 안내가 표시됩니다.

### 예상 데이터 크기

- 하단에 예상 데이터 행 수가 표시됩니다.
- 계산 기준: 기간 수 × 업종 수 × 광고상품 수 × 타겟팅 옵션 수

### 데이터셋 생성 요청

- "데이터셋 생성 요청" 버튼을 클릭하면 데이터 추출이 시작됩니다.
- 생성 성공 시 목록 화면으로 자동 이동합니다.
- 조회 조건에 맞는 데이터가 0건이면 생성 요청 버튼이 비활성화됩니다.

---

## 참고 사항

- 각 스텝에서 "이전" 버튼을 클릭하면 입력값이 유지된 채 이전 단계로 돌아갑니다.
- "취소" 버튼을 클릭하면 목록 화면으로 즉시 이동합니다.
- 스텝별 필수 항목을 모두 입력해야 "다음" 버튼이 활성화됩니다.`
    },
    {
      id: 'datashot-manage',
      title: '데이터셋 관리',
      slug: 'datashot-manage',
      updatedAt: '2026-05-28',
      content: `# 데이터셋 관리

DataShot 목록 화면에서 생성된 데이터셋을 조회하고 관리할 수 있습니다.

## 데이터셋 목록

### 목록 조회

- 생성된 데이터셋이 테이블 형태로 표시됩니다.
- 표시 항목: ID, 데이터셋명, 매체, 업종, 조회기간, 상태, 생성자, 생성일시
- 기본 정렬: 생성일시 최신순

### 상태 구분

| 상태 | 설명 | 상세 진입 |
|---|---|---|
| Completed | 데이터 추출 완료 | 가능 (행 클릭) |
| Processing | 데이터 추출 진행 중 | 불가 |
| Pending | 추출 대기 중 | 불가 |
| Error | 추출 실패 | 불가 |
| Expired | 생성 후 1년 경과로 만료 | 불가 |

### 검색

- 검색 버튼을 클릭하면 검색 입력창이 확장됩니다.
- 데이터셋명과 생성자를 대상으로 실시간 필터링됩니다.
- 대소문자를 구분하지 않습니다.

### 필터

- 필터 버튼을 클릭하면 필터 드롭다운이 열립니다.
- 상태 필터: Completed, Processing, Pending, Error, Expired (다중 선택)
- 매체 필터: Google Ads, Meta, kakao모먼트, 네이버 성과형 DA, 네이버 보장형 DA, TikTok (다중 선택)
- 상태와 매체 필터는 AND 조건으로 교차 적용됩니다.
- "필터 초기화" 버튼으로 모든 필터를 해제할 수 있습니다.

### 정렬

- 테이블 헤더를 클릭하면 해당 컬럼 기준으로 정렬됩니다.
- 같은 헤더를 다시 클릭하면 오름차순/내림차순이 전환됩니다.

### 페이지네이션

- 페이지당 표시 개수를 10, 20, 50 중 선택할 수 있습니다.
- 페이지 번호 또는 이전/다음 버튼으로 페이지를 이동합니다.

---

## 데이터셋 이동

데이터셋을 다른 Slot으로 이동할 수 있습니다.

### 이동 방법

1. 목록에서 이동할 데이터셋을 체크박스로 선택합니다. (다중 선택 가능)
2. 상단 액션바의 "이동" 버튼을 클릭합니다.
3. 또는 개별 데이터셋의 더보기(⋮) 메뉴에서 "이동"을 선택합니다.
4. 이동 다이얼로그에서 대상 Slot을 선택합니다.
5. "이동" 버튼을 클릭하면 이동이 완료됩니다.

### 이동 제약 사항

- 동일 광고주에 속한 Slot으로만 이동할 수 있습니다.
- Admin, Marketer 역할만 이동 기능을 사용할 수 있습니다.

---

## 데이터셋 복제

기존 데이터셋의 조회 조건을 상속하여 새 데이터셋을 생성할 수 있습니다.

### 복제 방법

1. 복제할 데이터셋의 더보기(⋮) 메뉴에서 "복제"를 선택합니다.
2. 기존 조회 조건이 사전 입력된 상태로 데이터셋 생성 화면에 진입합니다.
3. 필요에 따라 조건을 수정한 후 새 데이터셋을 생성합니다.

### 복제 참고 사항

- 만료(Expired) 상태의 데이터셋도 복제할 수 있습니다.
- 복제 시 동일한 조건으로 최신 데이터를 다시 추출할 수 있습니다.
- Admin, Marketer 역할만 복제 기능을 사용할 수 있습니다.

---

## 데이터셋 삭제

불필요한 데이터셋을 삭제할 수 있습니다.

### 삭제 방법

1. 목록에서 삭제할 데이터셋을 체크박스로 선택합니다. (다중 선택 가능)
2. 상단 액션바의 "삭제" 버튼을 클릭합니다.
3. 또는 개별 데이터셋의 더보기(⋮) 메뉴에서 "삭제"를 선택합니다.
4. 삭제 확인 다이얼로그에서 "삭제" 버튼을 클릭합니다.

### 삭제 주의 사항

- 삭제된 데이터셋은 복원할 수 없습니다.
- 삭제 시 해당 데이터셋의 Quota가 회복됩니다.
- Admin, Marketer 역할만 삭제 기능을 사용할 수 있습니다.`
    },
    {
      id: 'datashot-result',
      title: '데이터셋 결과',
      slug: 'datashot-result',
      updatedAt: '2026-05-28',
      content: `# 데이터셋 결과

데이터셋 목록에서 Completed 상태의 데이터셋을 클릭하면 상세 결과 화면으로 진입합니다.

## 헤더 영역

### 조회조건 요약

화면 상단에 이 데이터셋이 어떤 조건으로 추출되었는지 한눈에 확인할 수 있습니다.

- 매체명
- 조회기간 (시작 → 종료)
- 업종 수 — 클릭하면 선택된 업종 목록을 상세히 확인할 수 있습니다.
- 조회조건 수 — 클릭하면 광고상품·타겟팅 옵션 등 상세 설정을 확인할 수 있습니다.
- 지표 수 — 클릭하면 선택된 지표 목록을 그룹별로 확인할 수 있습니다.

### 액션 버튼

- **Share**: 링크 복사 또는 CSV 다운로드
- **Info**: 데이터셋 설명, ID, 생성일시, 생성자 정보 확인
- **More(⋮)**: 복제, 이동, 삭제 (Admin, Marketer만 표시)

---

## 데이터 테이블

### Extracted Data (추출 데이터)

추출된 데이터가 테이블 형태로 표시됩니다.

### 컬럼 구성

테이블 컬럼은 좌측 조건 컬럼과 우측 지표 컬럼으로 구분됩니다.

**조건 컬럼 (좌측)**
- 기간
- 매체
- 업종(대), 업종(중), 업종(소)
- 광고상품 컬럼 (매체별 동적 구성)
- 타겟팅 옵션 (선택한 경우)

**지표 컬럼 (우측)**
- 데이터셋 생성 시 선택한 지표들이 표시됩니다.
- 예: 광고비, 노출수, 클릭수, CTR, CPC, CPM, VTR 등

### 정렬

- 테이블 헤더를 클릭하면 해당 컬럼 기준으로 정렬됩니다.
- 같은 헤더를 다시 클릭하면 오름차순/내림차순이 전환됩니다.
- 숫자 컬럼은 숫자 비교, 텍스트 컬럼은 문자열 비교로 정렬됩니다.

### 필터

테이블 헤더 아래에 필터 행이 제공됩니다.

**목록형 필터 (조건 컬럼)**
- 드롭다운에서 다중 선택으로 필터링합니다.
- 드롭다운 내 검색 기능을 지원합니다.
- 전체 선택/해제가 가능합니다.

**지표 필터 (지표 컬럼)**
- 부등호(=, >, <, ≥, ≤)와 숫자 값을 조합하여 필터링합니다.
- 숫자 입력 시 1,000 단위 콤마가 자동 적용됩니다.

**필터 초기화**
- 필터가 적용된 상태에서 "필터 초기화" 버튼이 표시됩니다.
- 클릭 시 모든 필터가 해제됩니다.

### 페이지네이션

- 페이지당 표시 개수를 10, 20, 50, 100 중 선택할 수 있습니다. (기본: 20)
- 필터 적용 또는 페이지 크기 변경 시 1페이지로 자동 이동합니다.

---

## 데이터 표시 제한 안내

전체 데이터 행 수가 화면 표시 제한(1,000행)을 초과하는 경우, 상단에 경고 배너가 표시됩니다. 전체 데이터는 CSV 다운로드를 통해 확인할 수 있습니다.

---

## CSV 다운로드

### 다운로드 방법

1. 헤더의 Share 버튼을 클릭합니다.
2. "Export to CSV"를 선택합니다.

### 다운로드 참고 사항

- 화면의 필터/정렬 상태와 무관하게 전체 데이터가 다운로드됩니다.
- 파일명: \`DS_Dataset_Result_yyyymmdd.csv\` (다운로드 날짜 기준)
- 모든 역할(Admin, Marketer, Client, Agency)이 다운로드할 수 있습니다.

---

## 링크 공유

1. 헤더의 Share 버튼을 클릭합니다.
2. "Copy Link"를 선택합니다.
3. 현재 페이지 URL이 클립보드에 복사됩니다.

---

## 참고 사항

- 데이터셋 상세 화면은 읽기 전용입니다. 조회 조건이나 결과를 수정할 수 없습니다.
- Completed 상태의 데이터셋만 결과 화면에 진입할 수 있습니다. 그 외 상태(Processing, Pending, Error, Expired)는 목록에서 클릭할 수 없습니다.`
    }
  ]
}
